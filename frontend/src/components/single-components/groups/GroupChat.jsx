import React from "react";

export default function GroupChat({
  groupChatMessages,
  userId,
  groupChatName,
}) {
  return (
    <>
      <div className="groupChatContainer">
        <div className="groupChatName">{/* <h1>{group.name}</h1> */}</div>
        {/* map through the group chat messages */}
      </div>
    </>
  );
}
